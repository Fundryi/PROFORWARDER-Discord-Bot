require('dotenv').config();

module.exports = {
  botToken: process.env.BOT_TOKEN,
  debugMode: false, // Set to true to enable debug logging and test commands
  
  // Forward configurations - Define your message forwarding rules here
  forwardConfigs: [
    // Example same-server forwarding (basic):
    // {
    //   id: 1,
    //   name: "Announcements to General",
    //   sourceType: "discord",
    //   sourceServerId: "YOUR_SERVER_ID",
    //   sourceChannelId: "SOURCE_CHANNEL_ID",
    //   targetType: "discord",
    //   targetServerId: "YOUR_SERVER_ID", // Same server
    //   targetChannelId: "TARGET_CHANNEL_ID",
    //   enabled: true,
    //   allowEveryoneHereMentions: false, // Allow @everyone/@here forwarding
    //   createdBy: "USER_ID"
    // },
    
    // Example with AI Translation enabled (💡 RECOMMENDED SETUP):
    // {
    //   id: 2,
    //   name: "International News with Translation",
    //   sourceType: "discord",
    //   sourceServerId: "SOURCE_SERVER_ID",
    //   sourceChannelId: "SOURCE_CHANNEL_ID",
    //   targetType: "discord",
    //   targetServerId: "TARGET_SERVER_ID",
    //   targetChannelId: "TARGET_CHANNEL_ID",
    //   enabled: true,
    //   allowEveryoneHereMentions: false,
    //   createdBy: "USER_ID",
    //
    //   // AI Translation Configuration
    //   ai: {
    //     enabled: true,
    //     translation: {
    //       enabled: true,
    //       targetLanguages: ['es', 'fr', 'de'], // Spanish, French, German
    //       createThreads: true, // Creates Discord threads under forwarded messages
    //       provider: 'google', // Use free tier: 'google' or 'deepl'
    //       preserveFormatting: true, // Keep Discord formatting in translations
    //       notifyTranslations: false // Set to true for translation notifications
    //     },
    //     contentOptimization: {
    //       enabled: false, // Enable for advanced content enhancement (requires OpenAI)
    //       level: 'enhanced',
    //       platformSpecific: false
    //     }
    //   }
    // }
  ],
  
  // Telegram integration (optional)
  telegram: {
    enabled: false, // Set to true to enable Telegram integration
    botToken: process.env.TELEGRAM_BOT_TOKEN,
    apiUrl: process.env.TELEGRAM_API_URL || 'https://api.telegram.org'
  },
  
  // AI Integration for translation and content optimization
  ai: {
    enabled: false, // Set to true to enable AI features
    
    // Provider configurations (API keys from .env file)
    providers: {
      // OpenAI GPT-4 (💰 PAID SERVICE - Requires payment after free trial)
      // Best for: High-quality translations, content optimization
      // Cost: Pay per token (approximately $0.03/1K tokens for GPT-4)
      openai: {
        apiKey: process.env.OPENAI_API_KEY,
        model: 'gpt-4', // or 'gpt-3.5-turbo' for cheaper option
        maxTokens: 2000,
        temperature: 0.3 // Lower = more conservative, Higher = more creative
      },
      
      // Google Translate (🟡 FREEMIUM - Free tier: 500,000 chars/month)
      // Best for: Fast, reliable translations
      // Cost: Free tier available, then $20/1M characters
      google: {
        apiKey: process.env.GOOGLE_TRANSLATE_API_KEY,
        projectId: process.env.GOOGLE_PROJECT_ID
      },
      
      // DeepL (🟡 FREEMIUM - Free tier: 500,000 chars/month)
      // Best for: Professional-grade translation quality
      // Cost: Free tier available, then $6.99/month for Pro
      deepl: {
        apiKey: process.env.DEEPL_API_KEY,
        freeApi: true // Set to false if using DeepL Pro API
      }
    },
    
    // Translation settings
    translation: {
      enabled: true, // Enable translation features
      defaultProvider: 'google', // Recommended: 'google' or 'deepl' for free tier
      cacheTranslations: true, // Cache translations to reduce API costs
      maxCacheAge: 24 * 60 * 60 * 1000, // 24 hours in milliseconds
      fallbackProvider: 'deepl' // Fallback if primary provider fails
    },
    
    // Content optimization settings
    optimization: {
      defaultLevel: 'enhanced', // 'basic', 'enhanced', 'custom'
      preserveEmojis: true,
      preserveMentions: true,
      preserveLinks: true,
      maxOptimizationLength: 2000
    }
  }
};