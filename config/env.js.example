require('dotenv').config();

module.exports = {
  botToken: process.env.BOT_TOKEN,
  debugMode: false, // Set to true to enable debug logging and test commands
  
  // Forward configurations - Define your message forwarding rules here
  forwardConfigs: [
    // Example same-server forwarding:
    // {
    //   id: 1,
    //   name: "Announcements to General",
    //   sourceType: "discord",
    //   sourceServerId: "YOUR_SERVER_ID",
    //   sourceChannelId: "SOURCE_CHANNEL_ID",
    //   targetType: "discord",
    //   targetServerId: "YOUR_SERVER_ID", // Same server
    //   targetChannelId: "TARGET_CHANNEL_ID",
    //   enabled: true,
    //   createdBy: "USER_ID"
    // },
    
    // Example cross-server forwarding:
    // {
    //   id: 2,
    //   name: "News Cross-Server",
    //   sourceType: "discord",
    //   sourceServerId: "SOURCE_SERVER_ID",
    //   sourceChannelId: "SOURCE_CHANNEL_ID",
    //   targetType: "discord",
    //   targetServerId: "TARGET_SERVER_ID", // Different server
    //   targetChannelId: "TARGET_CHANNEL_ID",
    //   enabled: true,
    //   createdBy: "USER_ID"
    // }
  ],
  
  // Telegram integration (optional)
  telegram: {
    enabled: process.env.TELEGRAM_ENABLED === 'false',
    botToken: process.env.TELEGRAM_BOT_TOKEN,
    apiUrl: process.env.TELEGRAM_API_URL || 'https://api.telegram.org'
  },
  
  // AI integration for smart formatting (optional)
  ai: {
    enabled: process.env.AI_ENABLED === 'false',
    provider: process.env.AI_PROVIDER, // 'openai', 'anthropic', etc.
    apiKey: process.env.AI_API_KEY,
    model: process.env.AI_MODEL || 'gpt-3.5-turbo'
  },
  
  // Translation service (optional)
  translation: {
    enabled: process.env.TRANSLATION_ENABLED === 'false',
    provider: process.env.TRANSLATION_PROVIDER, // 'google', 'deepl', etc.
    apiKey: process.env.TRANSLATION_API_KEY,
    targetLanguages: (process.env.TRANSLATION_LANGUAGES || 'en,es,fr,de').split(',')
  }
};